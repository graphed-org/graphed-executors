# `graphed` is pinned to the same commit CI installs (env.GRAPHED in ../.github/workflows/ci.yml)
# so the rendered API reference matches the version these pages were written against.
# The [awkward,numpy] extras supply the backends that graphed-executors' autosummary imports at
# doc-build time.
graphed[awkward,numpy] @ git+https://github.com/graphed-org/graphed@49454d41e3b4fae05214ba67396b01fef9123f1a
